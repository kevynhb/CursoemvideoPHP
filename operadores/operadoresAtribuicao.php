<?php

    $c = $c + 5; 
    $c += 5; // mesma coisa que a a tribuição de cima.

    $b = $b + $a;
    $b += $a; // atribuição simplificada. também serve para otros operadores de atribuição.

    $a = $a + 1; 
    $a += 1; // ou 
    $a++; // faz a mesma coisa, soma mais 1.

?>